# Active environment: qa | uat | prod
ENV=qa

# Optional overrides - if set, these take precedence over the values in
# config/environments/<env>.json (useful for CI secrets injection)
# BASE_URL=
# API_KEY=
